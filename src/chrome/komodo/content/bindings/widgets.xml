<?xml version="1.0"?>
<!-- ***** BEGIN LICENSE BLOCK *****
 Version: MPL 1.1/GPL 2.0/LGPL 2.1
 
 The contents of this file are subject to the Mozilla Public License
 Version 1.1 (the "License"); you may not use this file except in
 compliance with the License. You may obtain a copy of the License at
 http://www.mozilla.org/MPL/
 
 Software distributed under the License is distributed on an "AS IS"
 basis, WITHOUT WARRANTY OF ANY KIND, either express or implied. See the
 License for the specific language governing rights and limitations
 under the License.
 
 The Original Code is Komodo code.
 
 The Initial Developer of the Original Code is ActiveState Software Inc.
 Portions created by ActiveState Software Inc are Copyright (C) 2000-2007
 ActiveState Software Inc. All Rights Reserved.
 
 Contributor(s):
   ActiveState Software Inc
 
 Alternatively, the contents of this file may be used under the terms of
 either the GNU General Public License Version 2 or later (the "GPL"), or
 the GNU Lesser General Public License Version 2.1 or later (the "LGPL"),
 in which case the provisions of the GPL or the LGPL are applicable instead
 of those above. If you wish to allow use of your version of this file only
 under the terms of either the GPL or the LGPL, and not to allow others to
 use your version of this file under the terms of the MPL, indicate your
 decision by deleting the provisions above and replace them with the notice
 and other provisions required by the GPL or the LGPL. If you do not delete
 the provisions above, a recipient may use your version of this file under
 the terms of any one of the MPL, the GPL or the LGPL.
 
 ***** END LICENSE BLOCK ***** -->

<!DOCTYPE bindings PUBLIC "-//MOZILLA//DTD XBL V1.0//EN" "http://www.mozilla.org/xbl" [
  <!ENTITY % bindingsDTD SYSTEM "chrome://komodo/locale/bindings.dtd">
  %bindingsDTD;
]>

<bindings xmlns="http://www.mozilla.org/xbl"
          xmlns:xbl="http://www.mozilla.org/xbl"
          xmlns:html="http://www.w3.org/1999/xhtml"
          xmlns:xul="http://www.mozilla.org/keymaster/gatekeeper/there.is.only.xul">

    <!-- Language list menupopup -->
    <binding id="lang-list">
        <content>
            <xul:menulist oncommand="document.getBindingParent(this).changedSelection(event);"
                          xbl:inherits="class, selected, flex, disabled">
                <xul:menupopup />
            </xul:menulist>
        </content>
        <implementation>
            <constructor>
                <![CDATA[
                    this.getHierarchy(); // fill in the menupopup
                ]]>
            </constructor>
            <destructor>
                <![CDATA[

                ]]>
            </destructor>
            <field name="_log">null</field>
            <property name="log">
                <getter>//<![CDATA[
                if (!this._log) {
                    var type = this.getAttribute('type');
                    this._log = ko.logging.getLogger("widgets.langList");
                }
                return this._log;
                //]]>
                </getter>
            </property>

            <method name="getHierarchy">
                <body>
                    <![CDATA[
                        var langService = Components.classes["@activestate.com/koLanguageRegistryService;1"].
                                    getService(Components.interfaces.koILanguageRegistryService);
                        var langHierarchy = langService.getLanguageHierarchy();
                        var popup = document.getAnonymousNodes(this)[0].firstChild;
                        var items = this.buildMenuTree(langHierarchy, true);
                        for (var i=0;i<items.length;i++)  {
                            popup.appendChild(items[i]);
                        }
                    ]]>
                </body>
            </method>
            <method name="buildMenuTree">
                <parameter name="hierarchy"/>
                <parameter name="toplevel"/>
                <body>
                    <![CDATA[
                        var menu;
                        var menupopup;
                        var menuitems = new Array();
                        var menuitem;
                        var children = new Object();
                        var count = new Object();
                        var i, j;
                        const XUL_NS = "http://www.mozilla.org/keymaster/gatekeeper/there.is.only.xul";

                        if (hierarchy.container == true)  {
                            // build menu
                            hierarchy.getChildren(children, count);
                            children = children.value;

                            for (i=0;i<children.length;i++)  {
                                menuitems.push(this.buildMenuTree(children[i], false));
                            }
                            if (!toplevel)  {
                                menu = document.createElementNS(XUL_NS, 'menu');
                                menupopup = document.createElementNS(XUL_NS, 'menupopup');
                                menupopup.setAttribute('class','menulist-menupopup');
                                menu.setAttribute('label', hierarchy.name);
                                menu.setAttribute('class', 'menulist-subLevel');
                                for (j=0;j<menuitems.length;j++)  {
                                    menupopup.appendChild(menuitems[j]);
                                }
                                menu.appendChild(menupopup);
                                return menu;
                            }
                            return menuitems;
                        }
                        else  {
                            menuitem = document.createElementNS(XUL_NS, 'menuitem');
                            menuitem.setAttribute('label', hierarchy.name);
                            return menuitem;
                        }
                    ]]>
                </body>
            </method>
            <method name="rebuildMenuTree">
                <parameter name="narrowToView"/>
                <parameter name="view"/>
                <body>
                    <![CDATA[
                        var popup = document.getAnonymousNodes(this)[0].firstChild;
                        while (popup.firstChild) {
                            popup.removeChild(popup.firstChild);
                        }
                        if (!narrowToView || !view) {
                            this.getHierarchy();
                            return;
                        }
                        try {
                            var currentLanguageNames = view.koDoc.languageObj.getSubLanguages({});
                            if (currentLanguageNames.indexOf(view.koDoc.language) == -1) {
                                currentLanguageNames.push(view.koDoc.language);
                            }
                            currentLanguageNames.sort()
                        } catch(ex) {
                            this.log.exception("Failed to get languages: " + ex);
                            this.getHierarchy();
                            return;
                        }
                        var menuitem;
                        const XUL_NS = "http://www.mozilla.org/keymaster/gatekeeper/there.is.only.xul";
                        for (var i = 0; i < currentLanguageNames.length; i++) {
                            menuitem = document.createElementNS(XUL_NS, 'menuitem');
                            menuitem.setAttribute('label', currentLanguageNames[i]);
                            popup.appendChild(menuitem);
                        }
                    ]]>
                </body>
            </method>
            <method name="changedSelection">
                <parameter name="event"/>
                <body>
                    <![CDATA[
                        var list = document.getAnonymousNodes(this)[0];
                        list.parentNode.setAttribute('data', this.selection);
                        list.setAttribute('label', event.originalTarget.getAttribute('label'));
                    ]]>
                </body>
            </method>
            <property name="selection">
                <setter>
                    <![CDATA[
                        var list = document.getAnonymousNodes(this)[0];
                        var item = document.getAnonymousElementByAttribute(list, 'label', val);
                        list.setAttribute('label', item.getAttribute('label'));
                    ]]>
                </setter>
                <getter>
                    <![CDATA[
                        var list = document.getAnonymousNodes(this)[0];
                        return list.getAttribute('label');
                    ]]>
                </getter>
            </property>
        </implementation>
    </binding>

    <!-- XSLT URLBar styling -->
    <binding id="double-box">
        <content>
            <xul:box class="db-internal-box" flex="1">
                <children/>
            </xul:box>
        </content>
    </binding>

    <binding id="kotooltip" extends="chrome://global/content/bindings/popup.xml#popup">
        <content>
        <children>
            <xul:description multiline="true" class="tooltip-label" xbl:inherits="value=label,crop" crop="right" flex="1"/>
        </children>
        </content>
    </binding>


    <!-- TODO: add a "check path validity" icon -->

    <binding id="listbox-patheditor">
        <content type="directory" orient="vertical">
            <xul:hbox style="margin-right: 5px">
                <xul:spring flex="1"/>
                <xul:button class="list-item-add-icon button-toolbar-a"
                            anonid="addDir"
                            oncommand="document.getBindingParent(this).add()"
                            tooltiptext="&add.tooltiptext;"/>
                <xul:button class="list-item-remove-icon button-toolbar-a"
                            anonid="deleteDir"
                            oncommand="document.getBindingParent(this).delete()"
                            tooltiptext="&delete.tooltiptext;"/>
                <xul:button class="list-item-up-icon button-toolbar-a"
                            anonid="moveUpDir"
                            oncommand="document.getBindingParent(this).moveUp()"
                            tooltiptext="&moveUp.tooltiptext;"/>
                <xul:button class="list-item-down-icon button-toolbar-a"
                            anonid="moveDownDir"
                            oncommand="document.getBindingParent(this).moveDown()"
                            tooltiptext="&moveDown.tooltiptext;"/>
            </xul:hbox>
            <xul:listbox
                flex="1"
                seltype="multiple"
                type="editable"
                onclick="document.getBindingParent(this).updateButtons()"
                xbl:inherits="seltype,rows">
                <children/>
            </xul:listbox>
        </content>
        <handlers>
            <handler event="keypress">
                <![CDATA[
                    this.updateButtons();
                ]]>
            </handler>
        </handlers>
        <implementation>
            <property name="value"
                      onset="this.setData(val);"
                      onget="return this.getData();"/>
            <field name="_log">null</field>
            <property name="log">
                <getter>//<![CDATA[
                if (!this._log) {
                    var type = this.getAttribute('type');
                    this._log = ko.logging.getLogger("widgets.pathEditor");
                }
                return this._log;
                //]]>
                </getter>
            </property>

            <method name="onpageload">
                <parameter name="prefwindow"/>
                <body>
                <![CDATA[
                try {
                    this.updateButtons();
                } catch (e) {
                    this.log.exception(e);
                }
                ]]>
                </body>
            </method>

            <method name="init">
                <parameter name="prefwindow"/>
                <body>
                <![CDATA[
                try {
                    var listbox = document.getAnonymousNodes(this)[1];
                    if (listbox.getRowCount()) {
                        listbox.selectItem(listbox.getItemAtIndex(0));
                    }
                    this.updateButtons();
                } catch (e) {
                    this.log.exception(e);
                }
                ]]>
                </body>
            </method>

            <method name="setCwd">
                <parameter name="cwd"/>
                <body>
                    <![CDATA[
                        this.cwd = cwd;
                    ]]>
                </body>
            </method>

            <method name="setData">
                <parameter name="encpathlist"/>
                <body>
                    <![CDATA[
                    try {
                        var listbox = document.getAnonymousNodes(this)[1];
                        var os = Components.classes["@activestate.com/koOs;1"].getService(Components.interfaces.koIOs);
                        var paths = encpathlist.split(os.pathsep);
                        var listitem;
                        while (listbox.firstChild) {
                            listbox.removeChild(listbox.firstChild);
                        }
                        for (var i = 0; i < paths.length; i++) {
                            listitem = document.createElement('listitem');
                            listitem.setAttribute('type', 'editable');
                            listitem.setAttribute('crop', 'center');
                            listitem.setAttribute('label', paths[i]);
                            listbox.appendChild(listitem);
                        }
                    } catch (e) {
                        this.log.exception(e);
                    }
                    ]]>
                </body>
            </method>

            <method name="deleteEmptyEntries">
                <body>
                    <![CDATA[
                    try {
                        var listbox = document.getAnonymousNodes(this)[1];
                        var listitem = listbox.lastChild;
                        var prev;
                        while (listitem) {
                            prev = listitem.previousSibling;
                            if (!listitem.getAttribute('label')) {
                                listbox.removeChild(listitem);
                            }
                            listitem = prev;
                        }
                    } catch (e) {
                        this.log.exception(e);
                    }
                    ]]>
                </body>
            </method>

            <method name="getData">
                <body>
                    <![CDATA[
                        var listbox = document.getAnonymousNodes(this)[1];
                        var os = Components.classes["@activestate.com/koOs;1"].getService(Components.interfaces.koIOs);
                        var paths = []
                        for (var i = 0; i < listbox.getRowCount(); i++) {
                            paths.push(listbox.getItemAtIndex(i).getAttribute('label'));
                        }
                        return paths.join(os.pathsep);
                    ]]>
                </body>
            </method>

            <method name="updateButtons">
                <body>
                    <![CDATA[
                    try {
                        var listbox = document.getAnonymousNodes(this)[1];
                        var deleteDir = document.getAnonymousElementByAttribute(this, "anonid", 'deleteDir');
                        var moveUpDir = document.getAnonymousElementByAttribute(this, "anonid", 'moveUpDir');
                        var moveDownDir = document.getAnonymousElementByAttribute(this, "anonid", 'moveDownDir');
                        if (listbox.selectedCount > 0) {
                            deleteDir.removeAttribute('disabled');
                        } else {
                            deleteDir.setAttribute('disabled', 'true');
                        }
                        if (listbox.selectedCount == 1 && listbox.selectedIndex != listbox.getRowCount()-1) {
                            moveDownDir.removeAttribute('disabled');
                        } else {
                            moveDownDir.setAttribute('disabled', 'true');
                        }
                        if (listbox.selectedCount == 1 && listbox.selectedIndex != 0) {
                            moveUpDir.removeAttribute('disabled');
                        } else {
                            moveUpDir.setAttribute('disabled', 'true');
                        }
                        if (this.getAttribute('oncommand')) {
                            eval(this.getAttribute('oncommand'));
                        }
                    } catch (e) {
                        this.log.exception(e);
                    }
                    ]]>
                </body>
            </method>
            <method name="add">
                <body>
                    <![CDATA[
                    try {
                        var listbox = document.getAnonymousNodes(this)[1];
                        var selectedItems = listbox.selectedItems;
                        var defaultDir = "";
                        var osPath = Components.classes["@activestate.com/koOsPath;1"].getService(Components.interfaces.koIOsPath);
                        var os = Components.classes["@activestate.com/koOs;1"].getService(Components.interfaces.koIOs);
                        var defaultDir;
                        if (selectedItems.length > 0) {
                            defaultDir = selectedItems[0].getAttribute('label');
                            defaultDir = osPath.expanduser(defaultDir);
                            if (! osPath.isabs(defaultDir)) {
                                // Need to join with curdir
                                if (this.cwd) {
                                    defaultDir = osPath.join(this.cwd,
                                                             defaultDir);
                                } else {
                                    defaultDir = null;
                                }
                            }
                        } else if (this.cwd) {
                            defaultDir = this.cwd;
                        } else {
                            defaultDir = null;
                        }
                        var prefName = "listbox.pathEditor.add"
                        if (!defaultDir) {
                            defaultDir = ko.filepicker.internDefaultDir(prefName);
                        } else {
                            defaultDir = osPath.normpath(defaultDir);
                        }
                        var path = null;
                        if (this.getAttribute('type') == 'directory') {
                            path = ko.filepicker.getFolder(defaultDir,
                                    "Select a folder to add to the list");
                        } else {
                            path = ko.filepicker.browseForFiles(defaultDir, null,
                                    "Select a folder to add to the list");
                        }
                        if (!path) return;
                        ko.filepicker.internDefaultDir(prefName, path);
                        this.deleteEmptyEntries();
                        var listitem = document.createElement('listitem');
                        listitem.setAttribute('type', 'editable');
                        listitem.setAttribute('label', path);
                        listbox.appendChild(listitem);
                        // need to ensure element is visible otherwise
                        // new listitem doesn't show as selected when
                        // selected.
                        listbox.ensureElementIsVisible(listitem);
                        listbox.selectItem(listitem);
                        this.updateButtons();
                    } catch (e) {
                        this.log.exception(e);
                    }
                    ]]>
                </body>
            </method>
            <method name="delete">
                <body>
                    <![CDATA[
                    try {
                        var listbox = document.getAnonymousNodes(this)[1];
                        var selectedItems = listbox.selectedItems;
                        for (var i = selectedItems.length-1; i >= 0; i--) {
                            listbox.removeChild(selectedItems[i]);
                        }
                        if (listbox.getRowCount() > 0) {
                            listbox.selectItem(listbox.getItemAtIndex(0));
                        }
                        this.updateButtons();
                    } catch (e) {
                        this.log.exception(e);
                    }
                    ]]>
                </body>
            </method>
            <method name="moveUp">
                <body>
                    <![CDATA[
                    try {
                        var listbox = document.getAnonymousNodes(this)[1];
                        var curItem = listbox.selectedItem;
                        curItem.parentNode.insertBefore(curItem,
                                                        curItem.previousSibling);
                        listbox.selectItem(curItem);
                        this.updateButtons();
                    } catch (e) {
                        this.log.exception(e);
                    }
                    ]]>
                </body>
            </method>
            <method name="moveDown">
                <body>
                    <![CDATA[
                    try {
                        var listbox = document.getAnonymousNodes(this)[1];
                        var curItem = listbox.selectedItem;
                        if (curItem.nextSibling && curItem.nextSibling.nextSibling) {
                            curItem.parentNode.insertBefore(curItem,
                                                            curItem.nextSibling.nextSibling);
                        } else {
                            listbox.appendChild(curItem);
                        }
                        listbox.selectItem(curItem);
                        this.updateButtons();
                    } catch (e) {
                        this.log.exception(e);
                    }
                    ]]>
                </body>
            </method>
        </implementation>
    </binding>

    <binding id="listbox-checklist">
        <content>
            <xul:vbox flex="1">
                <xul:hbox style="margin-right: 5px">
                    <xul:spring flex="1"/>
                    <xul:button class="list-item-add-icon button-toolbar-a"
                                id="addItem"
                                oncommand="document.getBindingParent(this).addItem()"
                                tooltiptext="&addEntry.tooltiptext;"/>
                    <xul:button class="list-item-remove-icon button-toolbar-a"
                                id="deleteItem"
                                oncommand="document.getBindingParent(this).deleteItems()"
                                tooltiptext="&deleteEntries.tooltiptext;"/>
                </xul:hbox>
                <xul:listbox
                    flex="1"
                    anonid="listbox"
                    style="min-height: 80px"
                    seltype="multiple"
                    rows="4"
                    pref="true"
                    type="editable"
                    onclick="document.getBindingParent(this).updateButtons()"
                    xbl:inherits="prefstring,flex,seltype,rows,style"/>
            </xul:vbox>
        </content>
        <handlers>
            <handler event="keypress">
                <![CDATA[
                    this.updateButtons();
                ]]>
            </handler>
        </handlers>
        <implementation>
            <field name="_log">null</field>
            <property name="log">
                <getter>//<![CDATA[
                if (!this._log) {
                    var type = this.getAttribute('type');
                    this._log = ko.logging.getLogger("widgets.checklist");
                }
                return this._log;
                //]]>
                </getter>
            </property>
            <method name="onpageload">
                <parameter name="prefwindow"/>
                <body>
                <![CDATA[
                try {
                    this.updateButtons();
                } catch (e) {
                    this.log.exception(e);
                }
                ]]>
                </body>
            </method>

            <property name="value"
                      onset="this.setData(val);"
                      onget="return this.getData();"/>

            <method name="init">
                <parameter name="prefwindow"/>
                <body>
                <![CDATA[
                try {
                    var listbox = document.getAnonymousElementByAttribute(this, "anonid", "listbox");
                    var rowcount = listbox.getRowCount();
                    if (rowcount) {
                        for (var i = 0; i < rowcount; i++) {
                            var item = listbox.getItemAtIndex(i);
                            item.setAttribute('type', 'checkbox');
                        }
                        listbox.selectAll();
                    }
                    this.updateButtons();
                } catch (e) {
                    this.log.exception(e);
                }
                ]]>
                </body>
            </method>

            <method name="saveprefs">
                <parameter name="prefwindow"/>
                <body> <![CDATA[
                    //dump("closing "+this.prefstring+"\n");
                    // should be nothing to do
                ]]> </body>
            </method>

            <method name="setData">
                <parameter name="listitems"/>
                <body>
                    <![CDATA[
                    try {
                        var listbox = document.getAnonymousElementByAttribute(this, "anonid", "listbox");
                        var listitem;
                        var items = listitems.split(':');
                        var item;
                        for (var i = 0; i < items.length; i++) {
                            if (!items[i]) continue;
                            item = items[i].split(',');
                            listitem = document.createElement('listitem');
                            listitem.setAttribute('type', 'checkbox');
                            listitem.setAttribute('label', item[1]);
                            listitem.setAttribute('checked', item[0]);
                            listbox.appendChild(listitem);
                        }
                    } catch (e) {
                        this.log.exception(e);
                    }
                    ]]>
                </body>
            </method>

            <method name="getData">
                <body>
                    <![CDATA[
                    try {
                        var listbox = document.getAnonymousElementByAttribute(this, "anonid", "listbox");
                        var items = [];
                        var item;
                        for (var i = 0; i < listbox.getRowCount(); i++) {
                            item = listbox.getItemAtIndex(i);
                            items.push(item.getAttribute('checked')+','+
                                       item.getAttribute('label'));
                        }
                        return items.join(':');
                    } catch (e) {
                        this.log.exception(e);
                        return [];
                    }
                    ]]>
                </body>
            </method>

            <method name="updateButtons">
                <body>
                    <![CDATA[
                    try {
                        var listbox = document.getAnonymousElementByAttribute(this, "anonid", "listbox");
                        var deleteItem = document.getAnonymousElementByAttribute(this, "id", 'deleteItem');
                        if (listbox.selectedCount > 0) {
                            deleteItem.removeAttribute('disabled');
                        } else {
                            deleteItem.setAttribute('disabled', 'true');
                        }
                        if (this.getAttribute('oncommand')) {
                            eval(this.getAttribute('oncommand'));
                        }
                    } catch (e) {
                        this.log.exception(e);
                    }
                    ]]>
                </body>
            </method>
            <method name="addItem">
                <body>
                    <![CDATA[
                    try {
                        var listbox = document.getAnonymousElementByAttribute(this, "anonid", "listbox");
                        var item = ko.dialogs.prompt("Module Name");
                        if (!item) return;
                        var listitem = document.createElement('listitem');
                        listitem.setAttribute('type', 'checkbox');
                        listitem.setAttribute('checked', 'true');
                        listitem.setAttribute('label', item);
                        listbox.appendChild(listitem);
                        // need to ensure element is visible otherwise
                        // new listitem doesn't show as selected when
                        // selected.
                        listbox.ensureElementIsVisible(listitem);
                        listbox.selectItem(listitem);
                        this.updateButtons();
                    } catch (e) {
                        this.log.exception(e);
                    }
                    ]]>
                </body>
            </method>
            <method name="deleteItems">
                <body>
                    <![CDATA[
                    try {
                        var listbox = document.getAnonymousElementByAttribute(this, "anonid", "listbox");
                        var selectedItems = listbox.selectedItems;
                        for (var i = selectedItems.length-1; i >= 0; i--) {
                            listbox.removeChild(selectedItems[i]);
                        }
                        if (listbox.getRowCount() > 0) {
                            listbox.selectItem(listbox.getItemAtIndex(0));
                        }
                        this.updateButtons();
                    } catch (e) {
                        this.log.exception(e);
                    }
                    ]]>
                </body>
            </method>
        </implementation>
    </binding>


    <binding id="tree-mappededitor">
        <content orient="vertical" flex="1">
            <xul:hbox style="margin-right: 5px">
                <xul:spring flex="1"/>
                <xul:button class="list-item-add-icon button-toolbar-a"
                            anonid="addDir"
                            oncommand="document.getBindingParent(this).add()"
                            tooltiptext="&add.tooltiptext;"/>
                <xul:button class="list-item-remove-icon button-toolbar-a"
                            anonid="deleteDir"
                            oncommand="document.getBindingParent(this).delete()"
                            tooltiptext="&delete.tooltiptext;"/>
                <xul:button class="list-item-up-icon button-toolbar-a"
                            anonid="moveUpDir"
                            oncommand="document.getBindingParent(this).moveUp()"
                            tooltiptext="&moveUp.tooltiptext;"/>
                <xul:button class="list-item-down-icon button-toolbar-a"
                            anonid="moveDownDir"
                            oncommand="document.getBindingParent(this).moveDown()"
                            tooltiptext="&moveDown.tooltiptext;"/>
            </xul:hbox>
            <xul:tree anonid="mapping" flex="1"
                      hidecolumnpicker="true"
                      onselect="document.getBindingParent(this).updateButtons();"
                      ondblclick="document.getBindingParent(this).edit();">
                <xul:treecols>
                    <xul:treecol id="name" label="&URI.label;" flex="1"/>
                    <xul:splitter class="tree-splitter"/>
                    <xul:treecol id="value" label="&mapsTo.label;" flex="1"/>
                </xul:treecols>
                <xul:treechildren flex="1" alternatingbackground="true" style="min-height: 100px">
                    <children/>
                </xul:treechildren>
            </xul:tree>
        </content>
        
        <handlers>
            <handler event="keypress">
                <![CDATA[
                    this.updateButtons();
                ]]>
            </handler>
        </handlers>
        <implementation>
            <property name="value"
                      onset="this.setData(val);"
                      onget="return this.getData();"/>

            <property name="tree"
                      onget="return document.getAnonymousElementByAttribute(this, 'anonid', 'mapping');"/>
            <property name="treeView"
                      onget="return this.tree.view;"/>
            <property name="treeChildren"
                      onget="return this.tree.lastChild;"/>
            <field name="_log">null</field>
            <property name="log">
                <getter>//<![CDATA[
                if (!this._log) {
                    var type = this.getAttribute('type');
                    this._log = ko.logging.getLogger("tree.mappedEditor");
                }
                return this._log;
                //]]>
                </getter>
            </property>

            <method name="onpageload">
                <parameter name="prefwindow"/>
                <body>
                <![CDATA[
                try {
                    this.updateButtons();
                } catch (e) {
                    this.log.exception(e);
                }
                ]]>
                </body>
            </method>

            <method name="init">
                <parameter name="prefwindow"/>
                <body>
                <![CDATA[
                try {
                    this.updateButtons();
                } catch (e) {
                    this.log.exception(e);
                }
                ]]>
                </body>
            </method>

            <method name="setCwd">
                <parameter name="cwd"/>
                <body>
                    <![CDATA[
                        this.cwd = cwd;
                    ]]>
                </body>
            </method>

            <method name="getCellText">
                <parameter name="row"/>
                <parameter name="col"/>
                <body>
                    <![CDATA[
                        if (row >=0 && row < this.treeView.rowCount) {
                            var item = this.treeChildren.childNodes[row].firstChild;
                            return item.childNodes[col].getAttribute('label');
                        }
                        return null;
                    ]]>
                </body>
            </method>

            <method name="setCellText">
                <parameter name="row"/>
                <parameter name="col"/>
                <parameter name="text"/>
                <body>
                    <![CDATA[
                        if (row >=0 && row < this.treeView.rowCount) {
                            var item = this.treeChildren.childNodes[row].firstChild;
                            item.childNodes[col].setAttribute('label', text);
                        }
                    ]]>
                </body>
            </method>

            <method name="setData">
                <parameter name="encpathlist"/>
                <body>
                    <![CDATA[
                    try {
                        // empty the tree
                        while (this.treeChildren.firstChild) {
                            this.treeChildren.removeChild(this.treeChildren.firstChild);
                        }

                        if (!encpathlist) return;
                        var paths = encpathlist.split('::');
                        for (var i = 0; i < paths.length; i++) {
                            var data = paths[i].split('##');
                            if (data.length == 2)
                                this.addRow(data[0],data[1]);
                        }
                    } catch (e) {
                        this.log.exception(e);
                    }
                    ]]>
                </body>
            </method>

            <method name="getData">
                <body>
                    <![CDATA[
                        var paths = []
                        for (var i = 0; i < this.treeView.rowCount; i++) {
                            var uri = this.getCellText(i, 0);
                            var path = this.getCellText(i, 1);
                            paths.push(uri + '##' + path);
                        }
                        return paths.join('::');
                    ]]>
                </body>
            </method>

            <property name="prefstring"
                      onget="return this.getAttribute('prefstring');"/>
            <property name="value"
                      onset="this.setData(val);"
                      onget="return this.getData();"/>
            <method name="onpageload">
                <parameter name="prefwindow"/>
                <body> <![CDATA[
                    //dump("loading "+this.prefstring+"\n");
                ]]> </body>
            </method>
            <method name="saveprefs">
                <parameter name="prefwindow"/>
                <body> <![CDATA[
                    //dump("closing "+this.prefstring+"\n");
                    // should be nothing to do
                ]]> </body>
            </method>

            <method name="updateButtons">
                <body>
                    <![CDATA[
                    try {
                        var deleteDir = document.getAnonymousElementByAttribute(this, "anonid", 'deleteDir');
                        var moveUpDir = document.getAnonymousElementByAttribute(this, "anonid", 'moveUpDir');
                        var moveDownDir = document.getAnonymousElementByAttribute(this, "anonid", 'moveDownDir');
                        var selCount = this.treeView.selection.count;
                        var index = this.treeView.selection.currentIndex;
                        var selItem = null;
                        if (this.treeView.rowCount < 0) {
                            index = -1;
                            selCount = -1;
                        } else if (index < 0 || index >= this.treeView.rowCount) {
                            index = 0;
                        }
                        if (index >= 0) {
                            selItem = this.treeChildren.childNodes[index];
                        }
                        
                        if (selCount > 0) {
                            deleteDir.removeAttribute('disabled');
                        } else {
                            deleteDir.setAttribute('disabled', 'true');
                        }
                        if (selCount == 1 && selItem.nextSibling) {
                            moveDownDir.removeAttribute('disabled');
                        } else {
                            moveDownDir.setAttribute('disabled', 'true');
                        }
                        if (selCount == 1 && selItem.previousSibling) {
                            moveUpDir.removeAttribute('disabled');
                        } else {
                            moveUpDir.setAttribute('disabled', 'true');
                        }
                        if (this.getAttribute('oncommand')) {
                            eval(this.getAttribute('oncommand'));
                        }
                    } catch (e) {
                        this.log.exception(e);
                    }
                    ]]>
                </body>
            </method>
            <method name="addRow">
                <parameter name="uri"/>
                <parameter name="localpath"/>
                <body>
                    <![CDATA[
                        var item = document.createElement('treeitem');
                        var row = document.createElement('treerow');
                        var uriCell = document.createElement('treecell');
                        var pathCell = document.createElement('treecell');
                        uriCell.setAttribute('crop', 'center');
                        uriCell.setAttribute('label', uri);
                        row.appendChild(uriCell);
                        pathCell.setAttribute('crop', 'center');
                        pathCell.setAttribute('label', localpath);
                        row.appendChild(pathCell);
                        item.appendChild(row);
                        this.treeChildren.appendChild(item);
                        return item;
                    ]]>
                </body>
            </method>

            <method name="add">
                <body>
                    <![CDATA[
                    try {
                        var defaultDir = this.getCellText(this.treeView.selection.currentIndex, 1);
                        if (defaultDir) {
                            var osPath = Components.classes["@activestate.com/koOsPath;1"].getService(Components.interfaces.koIOsPath);
                            defaultDir = osPath.expanduser(defaultDir);
                            if (! osPath.isabs(defaultDir)) {
                                // Need to join with curdir
                                if (this.cwd) {
                                    defaultDir = osPath.join(this.cwd,
                                                              defaultDir);
                                } else {
                                    defaultDir = '';
                                }
                            }
                            defaultDir = osPath.normpath(defaultDir);
                        }
                        var info = {
                            uri: null,
                            path: defaultDir
                        };
                        window.openDialog('chrome://komodo/content/dialogs/editPathMap.xul', '_blank', 'chrome,modal,titlebar,resizable,centerscreen', info);
                        if (!info.uri || !info.path) return;
                        var item = this.addRow(info.uri,info.path);
                        // need to ensure element is visible otherwise
                        // new listitem doesn't show as selected when
                        // selected.
                        this.tree.treeBoxObject.ensureRowIsVisible(this.treeView.rowCount-1);
                        this.treeView.selection.select(this.treeView.rowCount-1);
                        this.updateButtons();
                    } catch (e) {
                        this.log.exception(e);
                    }
                    ]]>
                </body>
            </method>
            <method name="edit">
                <body>
                    <![CDATA[
                    try {
                        var info = {
                            uri: this.getCellText(this.treeView.selection.currentIndex, 0),
                            path: this.getCellText(this.treeView.selection.currentIndex, 1)
                        };
                        window.openDialog('chrome://komodo/content/dialogs/editPathMap.xul', '_blank', 'chrome,modal,titlebar,resizable,centerscreen', info);
                        if (!info.uri || !info.path) return;
                        this.setCellText(this.treeView.selection.currentIndex, 0, info.uri);
                        this.setCellText(this.treeView.selection.currentIndex, 1, info.path);
                    } catch (e) {
                        this.log.exception(e);
                    }
                    ]]>
                </body>
            </method>

            <method name="delete">
                <body>
                    <![CDATA[
                    try {
                        for (var i = this.treeView.rowCount; i >= 0; i--) {
                            if (this.treeView.selection.isSelected(i)) {
                                this.treeChildren.removeChild(this.treeChildren.childNodes[i]);
                            }
                        }
                        if (this.treeView.rowCount > 0) {
                            this.tree.treeBoxObject.ensureRowIsVisible(0);
                            this.treeView.selection.select(0);
                        }
                        this.updateButtons();
                    } catch (e) {
                        this.log.exception(e);
                    }
                    ]]>
                </body>
            </method>
            <method name="moveUp">
                <body>
                    <![CDATA[
                    try {
                        var si = this.treeView.selection.currentIndex;
                        var curItem = this.treeChildren.childNodes[si];
                        if (curItem.previousSibling) {
                            curItem.parentNode.insertBefore(curItem,
                                                        curItem.previousSibling);
                            this.treeView.selection.select(si-1);
                        }
                        this.updateButtons();
                    } catch (e) {
                        this.log.exception(e);
                    }
                    ]]>
                </body>
            </method>
            <method name="moveDown">
                <body>
                    <![CDATA[
                    try {
                        var si = this.treeView.selection.currentIndex;
                        var curItem = this.treeChildren.childNodes[si];
                        if (curItem.nextSibling && curItem.nextSibling.nextSibling) {
                            curItem.parentNode.insertBefore(curItem,
                                                        curItem.nextSibling.nextSibling);
                            this.treeView.selection.select(si+1);
                        } else {
                            curItem.parentNode.appendChild(curItem);
                            this.treeView.selection.select(this.treeView.rowCount-1);
                        }
                        this.updateButtons();
                    } catch (e) {
                        this.log.exception(e);
                    }
                    ]]>
                </body>
            </method>
        </implementation>

    </binding>

    <!-- Widget used to display vertical text. -->
    <binding id="vertical_label"
             extends="chrome://global/content/bindings/general.xml#basecontrol">
        <content align="start">
            <xul:label anonid="label" class="label"/>
        </content>

        <implementation>
            <field name="_lastText">""</field>
            <field name="_label">document.getAnonymousElementByAttribute(this, "anonid", "label");</field>
            <constructor>
                setTimeout((function() {
                    this.value = this.getAttribute("short-label") || this.getAttribute("label");
                }).bind(this), 0);
                (new MutationObserver((function(mutations) {
                    for (let mutation of mutations) {
                        switch (mutation.attributeName) {
                            case "value":
                                this.value = this.getAttribute("value");
                                break;
                            case "collapsed":
                                // need to re-measure the text
                                this.refresh();
                                break;
                        }
                    }
                }).bind(this))).observe(this, {attributes: true,
                                        attributeFilter: ["value", "collapsed"]});
            </constructor>
            <method name="refresh">
                <!--
                  - Update the label
                  -->
                <body>
                <![CDATA[
                    // Insert a <box overflow=hidden><label/><box> somewhere
                    // visible so we can measure the text.  Note that we can't
                    // just stick it inside anonymous content here, since we might
                    // not be visible yet.
                    var doc = this.ownerDocument;
                    var box = doc.createElementNS(this.namespaceURI, "box");
                    box.setAttribute("style", "overflow: hidden; width: 0; height: 0;");
                    var measure = doc.createElementNS(this.namespaceURI, "label");
                    box.appendChild(measure);
                    doc.documentElement.appendChild(box);
                    measure.value = this._lastText;
                    var {width, height} = measure.getBoundingClientRect();
                    doc.documentElement.removeChild(box);

                    // Now that we have the dimensions, use it to set things
                    this._label.style.width = height + "px";
                    this._label.style.height = width + "px";
                    this.width = height;
                    this._label.value = this._lastText;
                    this.setAttribute("value", this._lastText);
                ]]>
                </body>
            </method>
            <property name="value">
                <getter>
                    return this._lastText;
                </getter>
                <setter>
                    if (val == this._lastText) return;
                    this._lastText = val;
                    this.refresh();
                </setter>
            </property>
        </implementation>

    </binding>


    <!-- Used to display file status within a menuitem. These "class" and
         "xbl:inherits" should match those in the "file-status-tab" binding.
    -->
    <binding id="menuitem-file-status" extends="chrome://global/content/bindings/menu.xml#menuitem">
        <content>
            <xul:hbox class="menu-file-status-image-box" align="start" pack="start" xbl:inherits="selected,disabled,checked">
                <xul:image class="file-status-icon" xbl:inherits="validate,disabled,file_status,tooltiptext=filestatustip"/>
                <xul:image class="file-status2-icon" xbl:inherits="validate,disabled,file_status2,tooltiptext=filestatus2tip"/>
                <xul:image class="native_file_image" xbl:inherits="validate,disabled,src=file_image_url,alt_image"/>
                <xul:image class="file-readonly-icon" xbl:inherits="validate,disabled,file_readonly,tooltiptext=locktip"/>
            </xul:hbox>
            <xul:label class="menu-iconic-text" flex="1" xbl:inherits="value=label,accesskey,crop" crop="right"/>
            <xul:hbox class="menu-accel-container" anonid="accel">
                <xul:label class="menu-iconic-accel" xbl:inherits="value=acceltext"/>
            </xul:hbox>
        </content>
    </binding>

</bindings>



