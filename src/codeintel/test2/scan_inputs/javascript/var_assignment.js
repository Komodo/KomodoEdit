
// JavaScript test cases to handle strange variable assignment cases

function test_scope() {
    // One very long and complex variable assignment statement
    var m_bInitializedEventHandlers = false,
        // Collection of visible menus
        m_oVisibleMenus = {},
        // Map of DOM event types to their equivalent CustomEvent types
        m_oEventTypes =  {
            "keydown": "keyDownEvent",
            "keyup": "keyUpEvent",
            "keypress": "keyPressEvent"
        },
        m_oFocusedMenuItem = null
    // This next variable should be global
    m_globalItem = 1

    var oEmphasisNode = this._getFirstElement(oAnchor),
        bEmphasis = false,
        bStrongEmphasis = false;

    var nGroupIndex = typeof p_nGroupIndex == "number" ? p_nGroupIndex : 0,
        aGroup = this._getItemGroup(nGroupIndex),
        oGroupItem;
}
