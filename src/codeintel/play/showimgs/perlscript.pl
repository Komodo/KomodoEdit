#!/usr/bin/perl -w
use strict;
use LWP::UserAgent;
use PerlModule qw(:all);

foo();

# To see AutoComplete images type
#    PerlModule::
# to see all except the "module" image and
#    LWP::
# to see the "module" image. Note that the "interface" and "namespace" images
# aren't relevant for Perl.

